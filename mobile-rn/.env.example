# Backend API base URL (no trailing slash). Used when EXPO_PUBLIC_API_URL is set.
# For local dev on same machine use your machine's IP (e.g. 192.168.x.x:8000) or use a tunnel.
# Example: EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1
EXPO_PUBLIC_API_URL=http://YOUR_IP:8000/api/v1
